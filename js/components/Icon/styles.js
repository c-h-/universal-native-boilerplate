import {
  StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
  icon: {
    // textAlign: 'center',
    // flexGrow: 1,
    width: 24,
    height: 24,
  },
});

export default styles;
